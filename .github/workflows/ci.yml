name: CI
on:
  push:
    branches: [main, "impl/**"]
  pull_request:

jobs:
  linux:
    name: linux-${{ matrix.compiler }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        compiler: [gcc, clang]
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      # The -dev packages are what CMake's find_library(EGL/GLESv2) needs: the
      # runtime packages ship only libEGL.so.1, so without them every
      # EGL-backed test target is silently never created and ctest runs 6
      # tests instead of the full suite.
      - name: Install toolchain and a software GL device
        run: |
          sudo apt-get update
          sudo apt-get install -y ninja-build clang \
            libegl1 libgles2 libegl-mesa0 mesa-utils \
            libegl-dev libgles-dev
      - name: Configure
        run: |
          if [ "${{ matrix.compiler }}" = "clang" ]; then
            export CC=clang CXX=clang++
          else
            export CC=gcc CXX=g++
          fi
          cmake -S . -B build -G Ninja
      - name: Build
        run: cmake --build build -j "$(nproc)"
      - name: Test
        # The translator device phase skips (rc 77) without a usable GPU;
        # llvmpipe usually provides one on the runner.
        run: cd build && ctest --output-on-failure

  asan:
    name: linux-gcc-asan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      # See the note in the linux job: the -dev packages are what make the
      # EGL-backed tests exist at all.
      - name: Install toolchain and a software GL device
        run: |
          sudo apt-get update
          sudo apt-get install -y ninja-build \
            libegl1 libgles2 libegl-mesa0 libegl-dev libgles-dev
      - name: Configure (ASan+UBSan, no LTO)
        run: >
          cmake -S . -B build -G Ninja -DSFPEW_LTO=OFF
          -DCMAKE_CXX_FLAGS="-fsanitize=address,undefined -fno-sanitize-recover=all"
          -DCMAKE_C_FLAGS="-fsanitize=address,undefined"
          -DCMAKE_SHARED_LINKER_FLAGS="-fsanitize=address,undefined"
          -DCMAKE_EXE_LINKER_FLAGS="-fsanitize=address,undefined"
      - name: Build
        run: cmake --build build -j "$(nproc)"
      - name: Test
        env:
          ASAN_OPTIONS: detect_leaks=0
        run: cd build && ctest --output-on-failure
